-- use ggc (linewise) or gbc (blockwise) for comments, in both normal and visual mode
-- dot repeatable
-- can be used with motions (gco, gc}, etc.)
return {
  'numToStr/Comment.nvim',
  opts = {},
}
